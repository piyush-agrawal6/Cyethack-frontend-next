export const metadata = {
    title: "Container Security",
    description: "Secure your containerized applications (Docker, Kubernetes). Cyethack ensures security throughout the container lifecycle.",
};

export default function ContainerSecurityLayout({ children }) {
    return <>{children}</>;
}
